/* app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Custom global styles (optional: add as needed for the app) */
@layer base {
  html {
    font-family: system-ui, -apple-system, sans-serif;
    line-height: 1.5;
  }
  
  body {
    @apply bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100;
    margin: 0;
    padding: 0;
  }
  
  /* Smooth scrolling for the app */
  html {
    scroll-behavior: smooth;
  }
}

/* Utility classes for AI response display (e.g., code blocks) */
@layer components {
  .code-block {
    @apply bg-gray-100 dark:bg-gray-800 p-4 rounded-lg overflow-x-auto font-mono text-sm;
  }
  
  .loading-spinner {
    @apply border-4 border-gray-200 border-t-blue-500 rounded-full animate-spin;
  }
}
